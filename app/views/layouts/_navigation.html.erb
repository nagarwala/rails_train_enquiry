<nav class="navbar navbar-expand-lg navbar-light bg-light">
<%= link_to "NTES", root_path, class: "navbar-brand", id: "logo" %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Trains
            </a>
            <% if user_signed_in? %>
            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
              <%= link_to "Enter new train details", new_train_path, class: "dropdown-item" if @current_user.admin? %>
              <%= link_to "Add station detail to train", new_train_station_path, class: "dropdown-item" if @current_user.admin? %>
              <%= link_to "View Trains", trains_path, class: "dropdown-item" %>
            <% end %>
      </li>
      <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Stations
            </a>
            <% if user_signed_in? %>
              <div class="dropdown-menu" aria-labelledby="navbarDropdown">
              <%= link_to "Enter new Station details", new_station_path, class: "dropdown-item" if @current_user.admin? %>
              <%= link_to "View Stations", stations_path, class: "dropdown-item" %>
              </div>
            <% end %>
      </li>
    </ul>
  </div>
</nav>